# No input validation library detected
This code uses Zod to validate input data, ensuring that only properly structured data is processed.
```typescript
import { z } from 'zod'; const inputSchema = z.object({ text: z.string().min(1) }); export async function POST(req) { const parsed = inputSchema.safeParse(await req.json()); if (!parsed.success) return new Response('Invalid input', { status: 400 }); // Your existing logic here }
```
_Generated by ShipCheck — review before merge._